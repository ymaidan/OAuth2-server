{
  "name": "oauth2-server-demo",
  "version": "1.0.0",
  "description": "OAuth2 Authorization Code flow demo with mock server, Express client, and automated tests",
  "type": "module",
  "scripts": {
    "start:oauth": "node src/oauth-server.js",
    "start:app": "node src/app-server.js",
    "test": "node --test tests/unit/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js"
  },
  "keywords": [
    "oauth2",
    "oauth2-mock-server",
    "authorization-code",
    "express",
    "nodejs"
  ],
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "oauth2-mock-server": "^8.2.3"
  }
}
